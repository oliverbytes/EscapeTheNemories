using UnityEngine;
using System.Collections;

public class RayCaster : MonoBehaviour 
{
    //private int platformLayerIndex = 1 << 8;
    //public GameObject rayOrigin;

    //void Update()
    //{
    //    Vector3 directionUp = transform.TransformDirection(Vector3.up);
    //    Vector3 rayFrom = rayOrigin.transform.position;
    //    rayFrom.y -= 50f;
    //    Ray ray = new Ray(rayFrom, directionUp);
    //    RaycastHit[] hits = Physics.RaycastAll(ray, 100f, platformLayerIndex);

    //    foreach (RaycastHit hit in hits)
    //    {
    //        //Debug.Log("tag: " + hit.transform.gameObject.tag);
    //    }
    //}
}
